export interface User {
    name: string; // required with minimum 5 characters
    designation: string;
    phone: string;
    email: string;
    // address: {
    // street: string; // required
    // postcode: string;
    // }
}